function Archive(id, title, description, applicant, status) {
    this.status = status
    this.title = title
    this.description = description
    this.applicant = applicant
    this.id = id
  };

export default Archive;  